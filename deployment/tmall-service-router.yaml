apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: tmall-service
  namespace: twisted-fate
spec:
  entryPoints:
    - web
  routes:
    - match: PathPrefix(`/tmall-service/`)
      kind: Rule
      services:
        - name: tmall
          port: 8090